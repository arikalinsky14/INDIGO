# Web layer for INDIGO inference. Install on top of the existing env that
# already carries torch + jax + jaxlayerlumos.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
# python-multipart is required for FastAPI's File / Form / UploadFile
# (CSV upload endpoint).
python-multipart>=0.0.7
